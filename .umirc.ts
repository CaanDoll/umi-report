import { defineConfig } from '@umijs/max';

export default defineConfig({
  qiankun: {
    master: {},
  },
  antd: {},
  model: {},
  initialState: {},
  request: {},
  layout: {
    title: '@umijs/max',
  },
  npmClient: 'pnpm',
});

